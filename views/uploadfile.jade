html
  head
    title fileMetadata pandalv
  body
    h3 Submit a file to view its filesize.
    form(action="/filemetadata" method="POST" enctype="multipart/form-data")
      input(type="file" name="file")
      button(type="submit") 提交
